<!DOCTYPE html>
<!-- This is the document type declaration, indicating that this is an HTML document -->
<html>
<!-- This is the root element of the HTML document -->
<head>
    <title>Awesome Test</title>
    <style>
        body {
            /* This selects the body element, which contains the content of the HTML document */
            background-color: #FF4500; 
            /* This sets the background color of the body element to a bright red */
            color: #800080; 
            /* This sets the text color of the body element to a purple color */
            text-align: center;
            /* This centers the text within the body element */
            font-family: Arial, sans-serif;
            /* This sets the font family for the text in the body element */
            padding: 20px;
            /* This adds a padding of 20 pixels around the content of the body element */
        }
        h1 {
            /* This selects all h1 elements, which are used for headings */
            margin-bottom: 20px;
            /* This adds a margin of 20 pixels to the bottom of each h1 element */
        }
        p {
            /* This selects all p elements, which are used for paragraphs                                                                                                                                                                                                                               */
            font-size: 18px;
            /* This sets the font size for the text in each p element */
            max-width: 800px;
            /* This sets the maximum width of each p element */
            margin: 0 auto;
            /* This centers each p element horizontally */
            line-height: 1.6;
            /* This sets the line height for the text in each p element */
        }
    </style>
</head>
<body>
    <h1>Welcome to my awesome test page!</h1>
    <!-- This is a heading element, which displays the text in a large font size -->
    <p>This is a simple PHP web page created by me.</p>
    <!-- This is a paragraph element, which displays the text in a normal font size -->
</body>
</html>
<!-- This ends the HTML document -->
a
a
a
a
a
a
a
aa
a
a
a
a
a
a
a
a
a
a
a

























































a
a
a
a
a
a
a
aa
a
a
a
a
a
